pipelines:
  secret-ops:
    group: platform
    label_template: "${git[:8]}"
    materials:
      git:
        git: "git@git.ai-traders.com:platform/secret-ops.git"
        branch: master
    stages:
      - test:
          clean_workspace: true
          secure_variables:
            VAULT_TOKEN: "AES:Ps0fk23o8C+TvBFA5g2gHQ==:nnmONtvAN86X2tIhGW15ESgy0IPZFZYJuMg46FOgorGLnZ4/6XCN0BWvo5/YPE3y"
          jobs:
            test:
              elastic_profile_id: w.c1.m1024.e5
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks verify_version
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks test
      - release:
          clean_workspace: true
          jobs:
            code:
              elastic_profile_id: w.c1.m1024.e5
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks release
      - publish:
          clean_workspace: true
          jobs:
            code:
              elastic_profile_id: w.c1.m1024.e5
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks publish
